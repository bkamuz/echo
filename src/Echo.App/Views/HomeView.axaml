<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:echo.App.ViewModels"
             x:Class="echo.App.Views.HomeView"
             x:DataType="vm:HomeViewModel"
             VerticalAlignment="Stretch">

  <StackPanel Spacing="16" VerticalAlignment="Top">
      <Border Classes="tile">
        <DockPanel>
          <Border DockPanel.Dock="Top" Classes="tile-header">
            <TextBlock Classes="section-title" Text="{DynamicResource Loc.Home.HotkeySection}"/>
          </Border>
          <StackPanel Classes="tile-body" Spacing="8">
            <TextBlock Text="{Binding HotkeyHint}"
                       FontFamily="{StaticResource MonoFont}"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource LinkBrush}"/>
            <TextBlock Classes="page-subtitle"
                       Text="{DynamicResource Loc.Home.HotkeyHint}"/>
          </StackPanel>
        </DockPanel>
      </Border>

      <Border Classes="tile">
        <DockPanel>
          <Border DockPanel.Dock="Top" Classes="tile-header">
            <TextBlock Classes="section-title" Text="{DynamicResource Loc.Home.ModelSection}"/>
          </Border>
          <StackPanel Classes="tile-body" Spacing="8">
            <TextBlock Text="{Binding ModelInfo}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource LinkBrush}"/>
            <TextBlock Classes="page-subtitle"
                       Text="{Binding ReadinessText}"/>
            <TextBlock Classes="page-subtitle"
                       Text="{Binding LastOutcome}"
                       IsVisible="{Binding ShowLastOutcome}"/>
          </StackPanel>
        </DockPanel>
      </Border>
  </StackPanel>
</UserControl>
